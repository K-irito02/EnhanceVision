# 06 国际化规范

## 文本管理

- QML 使用 `qsTr()`，C++ 使用 `tr()`
- 禁止硬编码用户可见字符串
- 同一语义保持统一翻译词汇

## 资源管理

- 翻译文件统一在 `resources/i18n/`
- 新增文案后必须更新 `ts/qm`
- 删除文案后清理废弃翻译项

## UI 影响检查

- 长文本溢出与换行
- 按钮/标题宽度膨胀
- 数字、单位、时间格式本地化

## 语言切换

- 切换后界面文本实时生效
- 不允许“部分翻译/部分未翻译”状态
