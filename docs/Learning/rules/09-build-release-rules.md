# 09 构建、发布与部署规范

## 构建规范

- 统一使用 `CMakePresets.json`
- 默认增量构建，禁止无故全量清理
- 配置与编译日志保留在 `logs/`

## 发布规范

- Release 包必须经过可运行验证
- Qt 依赖使用标准部署流程（如 `windeployqt`）
- 构建脚本和路径配置保持可复现

## 依赖与配置

- Qt 路径、编译器、架构在文档中固定说明
- 构建参数变更需同步文档与团队约定

## 质量门槛

- 不带已知构建错误进入主干
- 严禁“本机可用但流程不可复现”的提交
