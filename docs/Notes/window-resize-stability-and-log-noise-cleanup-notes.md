# 主窗口缩放稳定化与日志收敛说明

## 概述

本次迭代收尾了主窗口左边缘缩放抖动/黑缝问题，并同步清理了主窗口生命周期链路中不必要的运行期信息日志，避免继续向日志文件输出 `[INFO]` 类噪声。

**创建日期**: 2026-04-12

---

## 变更概述

### 修改文件

| 文件路径 | 修改内容 |
|----------|----------|
| `src/utils/WindowHelper.cpp` | 强制缩放过程重绘并抑制背景擦除，减少左边缘缩放的黑缝和闪动 |
| `src/utils/SubWindowHelper.cpp` | 同步主窗口的重绘策略，保持主/子窗口行为一致 |
| `src/core/LifecycleSupervisor.cpp` | 删除窗口生命周期链路中的冗余 `qInfo()` 信息日志 |
| `README.md` | 补充主窗口改为原生 `QQuickWindow` 宿主的说明 |
| `README_CN.md` | 补充主窗口改为原生 `QQuickWindow` 宿主的说明 |
| `docs/Learning/rules/10-testing-logging-rules.md` | 增加 UI 生命周期与窗口缩放阶段日志收敛约束 |
| `docs/Learning/rules/12-maintenance-documentation-rules.md` | 增加窗口宿主与日志收敛变更的文档同步要求 |
| `.agents/rules/10-skills-and-tools.md` | 增加运行期调试日志短期化约束 |

---

## 处理结果

- 主窗口左边缘拖拽缩放时的右半区抖动、黑缝、跳动现象已在前一轮修复基础上进一步收敛。
- 主窗口生命周期链路不再输出 `qInfo()` 级别的常规状态日志。
- 日志规范和维护规则已同步更新，后续类似问题需要先在代码与文档层同时收口。

---

## 日志核查结论

- 构建过程无编译错误。
- 当前可见警告主要来自“测试过程中强制结束进程”引发的异常退出标记，以及部分 AI/Vulkan 初始化信息，不属于本次主窗口缩放修复的阻塞问题。
- 没有发现需要单独修复的新增错误。

---

## 测试验证

| 场景 | 预期结果 | 实际结果 |
|------|----------|----------|
| 左边缘缩放主窗口 | 右半区不再脱离窗口，不出现明显黑缝和大幅闪动 | 已验证通过 |
| 右边缘缩放主窗口 | 保持平滑 | 已验证通过 |
| 重新构建项目 | 构建成功，无新增错误 | 已验证通过 |

