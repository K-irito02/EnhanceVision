---
alwaysApply: false
globs: ['**/*.qml', '**/*.cpp', '**/resources/i18n/**']
description: '国际化 - 文本抽取、翻译资源维护、运行时切换与UI适配'
---
# 国际化

## 语言范围

- 默认：`zh_CN`
- 支持：`en_US`

## 文本规则

- QML：`qsTr()` / `qsTranslate()`
- C++：`tr()`
- 禁止硬编码用户可见文本

## 资源规则

- 翻译文件统一在 `resources/i18n/`
- 新增/修改文案后必须更新 `.ts` 并生成 `.qm`
- 失效文案应及时清理，避免翻译垃圾项

## 切换规则

- 运行时切换语言后，界面需完成重翻译
- 不允许出现局部未翻译状态

## UI 适配检查

- 文本长度变化不破版
- 按钮、标题、提示支持换行与扩展
- 单位/时间/数量表达符合语言习惯

## 本文件边界

- 仅定义国际化规则
- 样式与排版规范见 `08-ui-design.mdc`
