---
alwaysApply: true
description: '规则治理总则 - 规则边界、优先级、冲突处理，始终生效'
trigger: always_on
---
# 规则治理总则

## 目标

- 保持代码统一、结构清晰、长期可维护
- 防止规则重复、冲突和无序扩张

## 规则优先级

1. `00-rule-governance.md`（治理总则）
2. `using-superpowers` 技能（技能使用总则，始终优先）
3. `02-architecture.md`（架构边界）
4. 语言/层内规范（`03`、`04`）
5. 领域规范（`05`~`09`）
6. 工具流程（`10`）

## 单一职责边界

- `01` 仅定义"项目事实基线"（技术栈、目录）
- `02` 仅定义"分层职责与数据流"
- `03` 仅定义 C++ 编码规范
- `04` 仅定义 QML/JS 编码规范
- `05` 仅定义构建与部署流程
- `06` 仅定义国际化
- `07` 仅定义性能与线程
- `08` 仅定义 UI 设计系统
- `09` 仅定义 Shader 规范
- `10` 仅定义技能与工具使用

## 冲突处理

- 若条款冲突：优先"边界规则"与"更具体的文件"
- 任何新规则必须避免与现有规则重复
- 修改规则时优先"合并到职责文件"，禁止横向复制

## 变更要求

- 规则变更需说明：原因、影响范围、迁移建议
- 功能完成后同步更新 `docs/Notes/` 与相关规则
- 新增技能时必须更新 `10-skills-and-tools.md` 和本规则优先级
- 技能使用原则变更时需同步更新 `using-superpowers` 技能文档
